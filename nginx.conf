events {
    worker_connections 1024;
}

http {

    server {
        listen       80 default_server;
        listen       [::]:80 default_server;
        server_name  pagai-api;
        root         /var/www/html;

        location / {
            include uwsgi_params;
            uwsgi_pass pagai:4000;
        }
    }
}